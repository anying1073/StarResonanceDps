<!--  Themes/MenuStyles.xaml  -->
<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:sys="clr-namespace:System;assembly=mscorlib">

    <!--  ===== 颜色（用自带 Alpha，避免多层透明叠加导致黑边） =====  -->
    <SolidColorBrush x:Key="MenuBg" Color="#FFF" />
    <SolidColorBrush x:Key="MenuFg" Color="#222" />
    <SolidColorBrush x:Key="MenuHover" Color="#EEE" />
    <SolidColorBrush x:Key="MenuPressed" Color="#F5F5F5" />
    <SolidColorBrush x:Key="MenuSep" Color="#14000000" />

    <!--  ===== 勾圈/箭头：参数化资源（改这里就能全局生效） =====  -->
    <!--  勾字符字号  -->
    <sys:Double x:Key="Menu.Check.FontSize">12</sys:Double>

    <!--  勾字符位置偏移(左,上,右,下)。第二个值为负=上移，正=下移；第一个/第三个值可左右移动  -->
    <Thickness x:Key="Menu.Check.GlyphMargin">0,0,0,0</Thickness>
    <!--  勾字符颜色  -->
    <SolidColorBrush x:Key="Menu.Check.Fg" Color="#222" />
    <!--  勾字符（可换成 ✅ 等）  -->
    <sys:String x:Key="Menu.Check.Glyph">&#xe686;</sys:String>

    <!--  箭头字符字号  -->
    <sys:Double x:Key="Menu.Arrow.FontSize">8</sys:Double>
    <!--  箭头位置偏移(左,上,右,下)。第二个值为负=上移；改第1/3个值可左右微调  -->
    <Thickness x:Key="Menu.Arrow.GlyphMargin">0,0,0,1</Thickness>
    <!--  箭头字符颜色  -->
    <SolidColorBrush x:Key="Menu.Arrow.Fg" Color="#222" />
    <!--  箭头字符（可换 ⟩、❯ 等看基线更顺眼的）  -->
    <sys:String x:Key="Menu.Arrow.Glyph">&#xe68e;</sys:String>
    <!--  箭头字符字体（影响基线/样式）  -->
    <FontFamily x:Key="Menu.Arrow.FontFamily">pack://application:,,,/Assets/Fonts/#Iconfont</FontFamily>

    <!--  圆角为“足够大”即可保持圆（宽高相等时）  -->
    <CornerRadius x:Key="Menu.CircleCorner">999</CornerRadius>
    <!--  圆角半径（一般不用改，保持超大即可）  -->


    <!--  不显示滚动条的 ScrollViewer 模板  -->
    <ControlTemplate x:Key="NoScrollViewerTemplate" TargetType="ScrollViewer">
        <ScrollContentPresenter />
    </ControlTemplate>

    <!--  ===== ContextMenu 外壳：圆角 + 投影 =====  -->
    <Style x:Key="ModernContextMenu" TargetType="ContextMenu">
        <Style.Triggers>
            <EventTrigger RoutedEvent="Opened">
                <BeginStoryboard>
                    <Storyboard>
                        <DoubleAnimation
                            Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                            From="0"
                            To="1"
                            Duration="0:0:0.2" />
                        <DoubleAnimation
                            Storyboard.TargetProperty="Opacity"
                            From="0"
                            To="1"
                            Duration="0:0:0.2" />
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
            <EventTrigger RoutedEvent="Closed">
                <BeginStoryboard>
                    <Storyboard>
                        <DoubleAnimation
                            Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                            From="1"
                            To="0"
                            Duration="0:0:0.2" />
                        <DoubleAnimation
                            Storyboard.TargetProperty="Opacity"
                            From="1"
                            To="0"
                            Duration="0:0:0.2" />
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
        </Style.Triggers>
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="HasDropShadow" Value="False" />
        <Setter Property="RenderTransform">
            <Setter.Value>
                <ScaleTransform ScaleX="1" ScaleY="0" />
            </Setter.Value>
        </Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ContextMenu">
                    <Border
                        Style="{StaticResource ShadowWindowBorder}"
                        Padding="0,5,0,5"
                        Background="{DynamicResource MenuBg}"
                        SnapsToDevicePixels="True">
                        <ScrollViewer
                                    Focusable="False"
                                    HorizontalScrollBarVisibility="Disabled"
                                    Template="{StaticResource NoScrollViewerTemplate}"
                                    VerticalScrollBarVisibility="Disabled">
                            <ItemsPresenter />
                        </ScrollViewer>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--  ===== Separator =====  -->
    <Style x:Key="ModernMenuSeparator" TargetType="Separator">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Separator">
                    <Border 
                        Height="1" 
                        Margin="5,2,5,2"
                        Background="{DynamicResource MenuSep}" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--  ===== MenuItem：Checkmark On Left / Arrow On Right  =====  -->
    <Style x:Key="ModernMenuItem" TargetType="MenuItem">
        <Setter Property="Foreground" Value="{DynamicResource MenuFg}" />
        <Setter Property="FontFamily" Value="pack://application:,,,/Assets/Fonts/#HarmonyOS Sans SC" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Height" Value="26" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="MenuItem">
                    <Border 
                        x:Name="Bg"
                        Padding="5,0,5,0"
                        Background="{DynamicResource MenuBg}">

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <!--  Left: Checkmark  -->
                                <ColumnDefinition Width="20" />
                                <!--  Center: Title  -->
                                <ColumnDefinition Width="*" />
                                <!--  Right: Arrow  -->
                                <ColumnDefinition Width="20" />
                            </Grid.ColumnDefinitions>

                            <!--  ✔ Checkmark  -->
                            <TextBlock
                                x:Name="CheckGlyph"
                                Grid.Column="0"
                                Margin="{DynamicResource Menu.Check.GlyphMargin}"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                FontFamily="pack://application:,,,/Assets/Fonts/#Iconfont"
                                FontSize="{DynamicResource Menu.Check.FontSize}"
                                Foreground="{DynamicResource Menu.Check.Fg}"
                                Text="{DynamicResource Menu.Check.Glyph}"
                                Visibility="Collapsed" />

                            <!--  Custom Icons (Selectable)  -->
                            <ContentPresenter
                                x:Name="LeftIcon"
                                Grid.Column="0"
                                Margin="0"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                ContentSource="Icon"
                                Visibility="Visible" />

                            <!--  Ttile  -->
                            <TextBlock
                                Grid.Column="1"
                                Margin="5,0,5,0"
                                VerticalAlignment="Center"
                                HorizontalAlignment="Center"
                                FontFamily="Sarasa UI SC"
                                FontSize="{Binding FontSize, RelativeSource={RelativeSource AncestorType=MenuItem}}"
                                Text="{Binding Header, RelativeSource={RelativeSource AncestorType=MenuItem}, StringFormat={}{0}}" />

                            <!--  › Arrow  -->
                            <TextBlock
                                x:Name="ArrowGlyph"
                                Grid.Column="2"
                                Margin="{DynamicResource Menu.Arrow.GlyphMargin}"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                FontFamily="{DynamicResource Menu.Arrow.FontFamily}"
                                FontSize="{DynamicResource Menu.Arrow.FontSize}"
                                Foreground="{DynamicResource Menu.Arrow.Fg}"
                                Text="{DynamicResource Menu.Arrow.Glyph}"
                                Visibility="Collapsed" />

                            <!--  SubMenu  -->
                            <Popup
                                x:Name="PART_Popup"
                                AllowsTransparency="True"
                                Focusable="False"
                                IsOpen="{TemplateBinding IsSubmenuOpen}"
                                Placement="Right"
                                PopupAnimation="Fade"
                                VerticalOffset="-5">

                                <Border
                                    Style="{StaticResource ShadowWindowBorder}"
                                    Padding="0,5,0,5"
                                    Background="{DynamicResource MenuBg}"
                                    SnapsToDevicePixels="True">
                                    <ScrollViewer
                                        Focusable="False"
                                        HorizontalScrollBarVisibility="Disabled"
                                        Template="{StaticResource NoScrollViewerTemplate}"
                                        VerticalScrollBarVisibility="Disabled">
                                        <ItemsPresenter />
                                    </ScrollViewer>
                                </Border>

                            </Popup>

                        </Grid>
                    </Border>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsHighlighted" Value="True">
                            <Setter TargetName="Bg" Property="Background" Value="{DynamicResource MenuHover}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Bg" Property="Background" Value="{DynamicResource MenuPressed}" />
                        </Trigger>

                        <!--  Icon 为空就折叠  -->
                        <DataTrigger Binding="{Binding Icon, RelativeSource={RelativeSource TemplatedParent}}" Value="{x:Null}">
                            <Setter TargetName="LeftIcon" Property="Visibility" Value="Collapsed" />
                        </DataTrigger>

                        <!--  ✅ 只有勾选时显示“勾”，并隐藏左侧图标  -->
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="LeftIcon" Property="Visibility" Value="Collapsed" />
                            <Setter TargetName="CheckGlyph" Property="Visibility" Value="Visible" />
                        </Trigger>

                        <Trigger Property="HasItems" Value="True">
                            <Setter TargetName="ArrowGlyph" Property="Visibility" Value="Visible" />
                        </Trigger>

                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.5" />
                        </Trigger>
                    </ControlTemplate.Triggers>

                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--  让某些 MenuItem 具备勾选能力的样式（默认不勾选）  -->
    <Style
        x:Key="CheckableMenuItem"
        BasedOn="{StaticResource ModernMenuItem}"
        TargetType="MenuItem">
        <Setter Property="IsCheckable" Value="True" />
        <!--  想点击后菜单不自动关闭可打开这行（当开关用很方便）  -->
        <Setter Property="StaysOpenOnClick" Value="True" />
    </Style>

</ResourceDictionary>
